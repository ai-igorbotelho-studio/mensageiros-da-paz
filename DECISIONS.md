# Registro de Decisões (DACI)

Toda decisão de trade-off final é do **Head** (sessão principal). Subagentes
recomendam; o Head decide e registra aqui.

Formato de cada entrada: **data · dono · decisão · trade-off sacrificado**.

| Data | Dono | Decisão | Trade-off sacrificado |
|------|------|---------|-----------------------|
| 2026-09-22 | Head | **Gate audit-design do /admin: APROVADO na 2ª rodada.** A 1ª auditoria BLOQUEOU com 5 falhas WCAG 2.2 AA reais (A.1 foco de teclado invisível em toda a base; A.2 toast de sucesso/erro sem live region; A.3 bug de `aria-labelledby` no `AdminInput` deixando a maioria dos campos sem nome acessível; A.4 modal de import sem semântica de diálogo/foco/trap; A.5 janela de undo de 7s não ajustável + perda silenciosa da reversibilidade ao trocar de aba). Todas corrigidas pelo frontend-multistack e re-verificadas no código: A.1 anel de foco (outline 2px Mulberry) no `PressableScale` + migração de todos os `Pressable` do AdminScreen; A.2 `accessibilityLiveRegion`/`role=status|alert` no toast; A.3 `aria-labelledby` só quando o label renderiza, senão `accessibilityLabel`, + nome em todos os campos; A.4 `role=dialog`+`aria-modal`, foco inicial/devolução, Escape e trap (`aria-hidden`+`inert`) no web / `accessibilityViewIsModal` no nativo; A.5 undo 7s→10s pausável em hover/foco e persistente à navegação. A.6 (Sage nunca como texto) travado por comentário preventivo. Pendências NÃO-bloqueantes delegadas ao **qa-cross-browser** (verificação visual, sem ambiente gráfico nesta sessão): contraste real do anel de foco em fundos variados, sem clipping por overflow, leitor de tela real (NVDA/VoiceOver) para anúncios do toast e nome dos campos, navegação por teclado ponta a ponta no modal e no undo, e reflow 320–1920px (transição 959→960px, painel B em ultrawide). Recomendação de robustez B.1: alinhar `AdminToast` para emitir `role=status` explícito nas variantes success/undo. | Reversibilidade e conformidade AA acima da simplicidade do build inicial; o gate independente segurou o deploy até as 5 falhas caírem |
| 2026-09-21 | Head | **Gate de design aprovado — build liberado + M1–M7 resolvidos.** Com os 5 documentos de spec prontos (`design/00`→`design/04`), o Head aprovou avançar para o build: design-system-engineer encoda a camada de tokens semânticos (aditiva, v1.0.0) + os 14 componentes; frontend-multistack constrói `AdminTwoPaneLayout`, o grid responsivo 320–1920px, a sidebar de desktop, o toast de undo real e migra `AdminScreen.tsx` incrementalmente; audit-design (+ qa-cross-browser) é o gate final. Resoluções: **M1–M6 aplicados de uma vez** (5 fontes abaixo do piso `minFontSize=17` que o próprio `tokens.ts` declara, + `primaryButton` trocando `colors.accent`/Sage por `colors.primary`, já que Sage é reservado a confirmação/publicado). **M7 (copy da exclusão em massa): botão "Excluir N itens" + toast "Desfazer"**, sem a frase "não dá para desfazer", que deixou de ser verdade com o undo temporário. `ADMIN_BG`/`ADMIN_SURFACE` sobem de `AdminScreen.tsx` para `tokens.ts` no mesmo PR (sem mudança visual). Storybook fica fora de escopo por ora (um só consumidor). | M1–M6: legibilidade/semântica de marca acima da estabilidade de não-mexer no código atual. M7: reforço textual de irreversibilidade — trocado por texto honesto (agora é reversível). Faseamento por tela — trocado por aplicar tudo junto (valores pequenos, piso já declarado) |
| 2026-09-21 | Head | **Fase Design & UX do /admin — 4 decisões estruturais aprovadas pelo Head antes do build.** No gate de arquitetura (docs `design/00`→`design/02`), o Head decidiu: (1) **Navegação desktop vira sidebar lateral** (Prática/Biblioteca/Guia) no lugar do segmented control em pílula, que continua só no mobile — mais espaço vertical, padrão de ferramenta de trabalho; muda a base estrutural de todo o layout ≥960px. (2) **Edição no desktop em 2 painéis lado a lado** (lista/overview ~38% + editor/detalhe ~62%, largura de leitura máx ~640–720px no painel direito); mobile segue coluna única empilhada. (3) **"Novo item / Publicação manual" movido para o topo da lista** (era depois da lista + bloco de seleção em massa) — ação mais comum do CMS ancorada no ponto de entrada; o Head já tinha ajustado esse fluxo antes, por isso a mudança foi confirmada explicitamente, não unilateral. (4) **Exclusão em massa ganha undo temporário** (toast "Desfazer" por alguns segundos antes de apagar de verdade) no lugar da exclusão imediata irreversível — alinha ao princípio de reversibilidade proporcional ao risco da visão de experiência. | (1) Coerência de ter a mesma navegação em todas as larguras — trocada por aproveitar o desktop. (2) Simplicidade da coluna única — trocada por editar sem perder contexto da lista. (3) Posição anterior do "Novo item" embaixo — revertida. (4) Simplicidade do apagar-imediato sem estado de undo — trocada por reversibilidade |
| 2026-09-21 | Head | **Texto lido ao vivo de Google Doc + Admin vira "Biblioteca" com link direto pra planilha de Práticas.** (1) Novo `FileType` `"gdoc"`: cola-se um link de Google Doc (`docs.google.com/document/d/...`) e o app busca o texto puro via URL de exportação (`.../export?format=txt`) toda vez que o item é aberto — editar o Doc atualiza o app sozinho, sem copiar/colar de novo no admin (`toGoogleDocsTextExportUrl` em `src/utils/driveUrl.ts`, usado em `ItemDetailScreen.tsx`). Documento precisa estar compartilhado como "Qualquer pessoa com o link". (2) Seção "Itens" do admin renomeada pra "Biblioteca", com texto explicando que mostra tudo publicado (e rascunhos) de cada uma das 4 categorias. (3) Link "Ver histórico completo na planilha →" adicionado à seção Prática da Semana, apontando pra planilha "Mensageiros da Paz" — o admin edita a prática atual ali mesmo, mas o histórico completo fica um clique de distância. | Nenhuma — todas essas são adições, nada removido |
| 2026-09-21 | Head | **Botão "Apagar todos" por categoria no admin — reset de conteúdo pra recadastrar tudo via Drive.** O Head decidiu que todo conteúdo (Orações/Músicas/Textos/Livros) vai ser cadastrado só com link do Google Drive daqui pra frente, e pediu pra apagar os registros atuais (incluindo as 4 duplicatas de "Guerreiro do Bem" da sessão anterior). Como a sessão de código não tem a senha de admin, não dá pra apagar direto no Firestore — adicionado um botão "Apagar todos de {categoria} (N)" na lista de Itens do `/admin`, com confirmação em duas etapas (primeiro clique pede confirmação, segundo clique executa) — evita apagar tudo sem querer. Roda dentro da sessão já autenticada de quem clicar, mesma lógica do antigo botão de importação inicial. | Nenhuma — ação destrutiva com confirmação explícita, dentro da sessão do próprio admin |
| 2026-09-21 | Head | **Bug grave: nenhum item aparecia em nenhuma categoria (app nem admin) — índice composto do Firestore nunca criado.** Confirmado testando a query real direto contra a API REST do Firestore: `category == X` combinado com `orderBy("order")` (usado tanto em `fetchItemsByCategory` quanto em `adminListItemsByCategory`) retorna `FAILED_PRECONDITION: The query requires an index`, silenciosamente tratado como "lista vazia" por quem chamava — por isso "Músicas" aparecia vazio no app E a lista do admin ficava vazia mesmo depois de cadastrar (o cadastro funcionava, só a listagem falhava). Corrigido removendo `orderBy` das duas queries e ordenando por `order` no cliente (`.sort()`) — elimina de vez a dependência de índice composto manual, sem precisar mexer no console do Firebase; cada categoria tem poucas dezenas de itens, ordenar em JS não tem custo relevante. Efeito colateral descoberto: como o admin "parecia" não estar salvando, o Head tentou cadastrar "Guerreiro do Bem" repetidas vezes — existem hoje 4 registros duplicados na coleção `items` (3 com link do Drive + o original do Cloudflare), a remover manualmente pelo admin depois que a listagem voltar a funcionar. | Nenhuma — corrige um bug crítico que derrubava a listagem pública inteira, sem nenhuma perda de funcionalidade |
| 2026-09-21 | Head | **Bug real: áudio/imagem do Google Drive não tocavam/exibiam — corrigido com conversão automática de URL.** Causa raiz: um link de compartilhamento padrão do Drive (`.../file/d/{id}/view?usp=sharing`) retorna uma página HTML de visualização, não os bytes do arquivo — `Audio.Sound.createAsync`/`<Image>` tentavam carregar essa URL como mídia direta e falhavam silenciosamente (cai no `ErrorState` de reprodução, ou imagem quebrada). PDF nunca teve esse problema porque abre externamente via `Linking.openURL` (o próprio Google trata a visualização). Corrigido com `src/utils/driveUrl.ts` (`toDirectFileUrl`): detecta link do Drive, extrai o id do arquivo, reescreve para `.../uc?export=view&id={id}` (formato de conteúdo direto) — aplicado em `ItemDetailScreen.tsx` antes de tocar áudio ou exibir imagem. Aviso adicionado no formulário do admin quando categoria Áudio + link do Drive são combinados, recomendando Cloudflare Pages como opção mais confiável para arquivos maiores (Drive pode mostrar tela de confirmação de vírus em arquivos grandes, sem streaming/range request garantido). | Nenhuma — corrige um bug real sem custo nem trade-off; a recomendação de usar Cloudflare para áudio continua valendo para arquivos grandes |
| 2026-09-21 | Head | **Botão "Importar conteúdo inicial" removido do `/admin`.** Já cumpriu a função (27 itens confirmados cadastrados — 8 orações, 18 livros, 1 música). Removidos o botão, os estados/handler no `AdminScreen.tsx` e `src/firebase/seedContent.ts` inteiro, para não sobrar risco de clique acidental duplicando os itens. | Nenhuma — era um utilitário de uso único, sem função depois de rodado |
| 2026-09-21 | Head | **`_redirects` rejeitado pelo Cloudflare Pages ("Infinite loop detected") — corrigido com flag de força.** Confirmado no log de build: `/* /index.html 200` foi parseado como 0 regras válidas e ignorado — o SPA fallback nunca chegou a ser aplicado, então acessar `/admin` (ou qualquer rota) direto por URL/recarregamento provavelmente retornaria 404 real do Cloudflare (só não quebrou ainda porque a navegação usada foi sempre client-side, clicando dentro do app já carregado). Corrigido trocando para `/* /index.html 200!` — o sufixo `!` força a regra sem a checagem de arquivo estático que o linter do Cloudflare interpreta erroneamente como loop infinito (workaround documentado para esse bug conhecido do Cloudflare Pages). | Nenhuma — mesma regra, sintaxe corrigida |
| 2026-09-21 | Head | **Histórico da Prática da Semana numa planilha Google, via Apps Script Web App.** O Firestore só guarda a prática atual (sobrescrita a cada edição) — sem histórico. O Head já criou a planilha "Mensageiros da Paz" (colunas Prática da semana · Inspiração · Data · Quem publicou). Implementado `src/integrations/practiceSheetSync.ts`: toda vez que o admin salva a Prática da Semana, além de gravar no Firestore (fonte real, lida pelo app), dispara um POST fire-and-forget pra um Google Apps Script Web App vinculado à planilha, que adiciona uma linha. Gratuito, sem Blaze/cartão. Novo campo `inspiration` no formulário do admin e no doc `config/practice_of_the_week` — **só a coluna "Prática da semana" (o campo `text`) aparece no app**; Inspiração fica só no registro interno/planilha, a pedido explícito do Head. URL do webhook configurável via `EXPO_PUBLIC_PRACTICE_SHEET_WEBHOOK_URL` (opcional — sem ela, a sincronização é só pulada, nunca bloqueia o salvamento real). Passo a passo completo do Apps Script em `docs/PRACTICE-SHEET-SETUP.md`. | Nenhuma — sincronização é best-effort e nunca compete com a gravação real no Firestore |
| 2026-09-21 | Head | **Conteúdo real cadastrável de dentro do próprio painel — botão "Importar conteúdo inicial".** Nenhum dos 8 textos de oração, 18 livros e a música real estava cadastrado no Firestore (só documentado em `docs/CONTENT-SEED.md`). Como a sessão de código não tem (e não deve pedir) a senha de admin, criado `src/firebase/seedContent.ts` com os dados reais embutidos e uma função `seedInitialContent` que roda dentro da sessão já autenticada de quem clicar — um botão novo no topo do `/admin` ("Importar conteúdo inicial"), visível só pra quem já está logado. Extraídos os textos completos das 8 orações do protótipo original (artboards `OracaoCaritas.dc.html` etc., artifact do Head). Estendido o modelo de dados: `ContentItem`/`ItemFormValues` ganham campo `text` (texto direto, opcional, independente de `source`) — as orações usam isso em vez de arquivo, exibido direto em `ItemDetailScreen`. Validação do formulário do admin relaxada: com texto preenchido, não exige mais link de arquivo/streaming. | Nenhuma — mecanismo evita compartilhar credenciais de admin com a sessão de código, e o botão é auto-descritivo (roda uma vez, avisa que rodar de novo duplica) |
| 2026-09-21 | Head | **Texto da Prática da Semana com largura máxima fixa.** Em telas largas (desktop), o texto esticava até as bordas da janela, ficando difícil de ler — sem `maxWidth`, o `Text` ocupa toda a largura disponível do flex container. Adicionado `maxWidth: 420` + `alignSelf: "center"` em `practiceText` (`HomeScreen.tsx`) — o texto quebra linha numa coluna fixa e centralizada, como um bloco de leitura normal, em qualquer largura de tela. | Nenhuma — corrige legibilidade em desktop sem afetar mobile |
| 2026-09-21 | Head | **Layout da Home ajustado de novo: espalhado pela altura da página, não compacto no centro.** O ScrollView compacto (entrada anterior) juntou tudo no meio da tela — o Head pediu o oposto: emblema fixo perto do topo, texto da prática numa área central fixa, botões perto do rodapé, ocupando a altura inteira da página. Trocado por três blocos `flex` (`emblemArea` flex:2, `textArea` flex:3, `buttonArea` flex:2) dentro de uma `View` (não `ScrollView`), cada um com seu próprio alinhamento — mantém os tamanhos reduzidos da correção anterior (fonte/botões menores) para não cortar, mas distribui os blocos ao longo da tela em vez de agrupá-los. | Nenhuma — ajuste fino de layout a pedido direto do Head |
| 2026-09-21 | Head | **Reorganização de layout da Home: tudo cabe na tela, título centralizado.** A pedido do Head (com marcações visuais no print: emblema pra cima, texto da prática numa "área verde", botões numa "área azul", título sempre centralizado): emblema reduzido (96×149 → 60×93) e movido para o topo; fonte do texto da prática reduzida (26→19, lineHeight 36→26) pra sempre caber sem cortar; botões reduzidos (`maxWidth` 220→200, altura -8, fonte 14→13, ícone 18→15, gaps menores); Home inteira envolvida num `ScrollView` (antes `View` com `justifyContent: space-between`, que cortava conteúdo em telas pequenas — layout de altura fixa nunca garante caber tudo). `headerTitleAlign: "center"` adicionado ao `RootNavigator` — título "Mensageiros da Paz" agora sempre centralizado no cabeçalho, em qualquer tela. | Nenhuma — corrige um bug real de overflow em tela pequena (conteúdo cortado, ilegível) e atende ao layout pedido |
| 2026-09-21 | Head | **OG image, meta tags de SEO/WhatsApp e novo ícone de loading.** (1) `app/public/og-image.png` (1200×630, gerado via PIL): fundo Pale apricot, emblema Chama Tripla, título "Mensageiros da Paz" e "Orações · Músicas · Textos · Livros" — imagem que aparece ao compartilhar o link no WhatsApp/redes. (2) O Expo SDK 51 não expõe `og:image`/`description` via `app.json`, e o nome do bundle JS muda a cada build (hash), então criado `app/scripts/inject-meta.js`, rodado como passo do `npm run build:web` (depois do `expo export`), que injeta as meta tags Open Graph/Twitter Card direto no `dist/index.html` gerado. (3) `LoadingState.tsx` trocado: em vez do `ActivityIndicator` genérico do sistema, agora mostra o emblema Chama Tripla parado no centro com um anel girando ao redor (`Animated.loop` + `rotate`, sem dependência nova). | Nenhuma — melhoria de SEO/compartilhamento e identidade visual mais consistente no loading, sem custo nem dependência nova |
| 2026-09-21 | Head | **Emoji removido — ícones dos botões viraram SVG desenhado à mão (`react-native-svg`), no estilo de linha do protótipo original.** O Head recusou emoji ("nunca usar emojis") e pediu o estilo de ícone minimalista de linha já usado no protótipo visual (`docs/UX-ARCHITECTURE.md`, artboards Oracoes.dc.html/Musicas.dc.html) — gota para Orações, nota musical para Músicas, linhas de texto para Textos, livro aberto para Livros. Implementado em `src/components/CategoryIcons.tsx` com `react-native-svg` (`Svg`/`Path`/`Circle`), sem depender de nenhuma fonte de ícone externa — SVG renderiza direto como elemento, sem risco de "tofu box" ou travamento. Botão ajustado para bater com a referência visual enviada (pílula, ícone colado ao texto, `gap` pequeno em vez de margem). Bundle web cresce só ~40KB (vs. ~2.5MB da fonte de ícone completa removida antes). | Nenhuma — ganho líquido: visual pedido, sem a fragilidade da fonte de ícone, bundle mais leve |
| 2026-09-21 | Head | **`@expo/vector-icons` removido — trocado por emoji Unicode nos botões da Home.** Causa raiz encontrada por depuração do bundle: a fonte de ícone é registrada internamente sob o nome de família `'material-community'` (não `'MaterialCommunityIcons'`) e depende de `FontFaceObserver` para confirmar o carregamento no web — mecanismo frágil que já causou dois bugs em produção seguidos (app travado esperando a fonte, depois ícones aparecendo como "tofu box" □ por falha silenciosa desse carregamento). Removida a dependência inteira (`@expo/vector-icons`, `expo-font`) e trocados os ícones dos botões (Orações/Músicas/Textos/Livros) por emoji Unicode (🙏🎵📄📖) — renderizam sempre, sem depender de nenhum carregamento de fonte externo. Também reduz o bundle web em ~2.5MB (fontes de ícone completas não eram mais necessárias). | Ícones em estilo "linha minimalista" (agora são emoji coloridos) — trocados por confiabilidade total de renderização, depois de dois incidentes de produção causados pela abordagem anterior |
| 2026-09-21 | Head | **Correção urgente: site travado em loading infinito (bug introduzido pela própria correção anterior de ícones).** A troca anterior (`App.tsx` com `useFonts` bloqueando a renderização até `MaterialCommunityIcons` carregar) quebrou o site inteiro em produção — a promise de `useFonts` não resolve de forma confiável no build web, deixando `LoadingState` preso pra sempre. Corrigido removendo o bloqueio: `useFonts` agora só dispara o carregamento em paralelo, sem condicionar a renderização do app a ele — os ícones aparecem assim que prontos, sem travar nada antes disso. Validado localmente (`tsc --noEmit` + `npm run build:web`) antes do push. | Ícones garantidamente visíveis desde o primeiro frame — trocado por nunca mais travar o app inteiro por causa de uma fonte de ícone que pode demorar/falhar a carregar |
| 2026-09-21 | Head | **Ajustes finos da Home: centralização, botões menores, ícones dos botões corrigidos, título vira botão de Home.** (1) Prática da semana e rótulo agora centralizados (antes alinhados à esquerda). (2) Botões de categoria reduzidos (`maxWidth` 280→220, padding/fonte menores). (3) Ícones dos botões não apareciam no deploy anterior — a fonte `MaterialCommunityIcons` não estava sendo carregada explicitamente na web; corrigido com `expo-font`/`useFonts` em `App.tsx` (mostra `LoadingState` até a fonte de ícone carregar). (4) O título "Mensageiros da Paz" no cabeçalho agora é clicável em qualquer tela e navega para a Home, funcionando como atalho de "home" a partir de qualquer lugar do app (`RootNavigator.tsx`, `headerTitle` customizado). | Nenhuma — ajustes visuais e correção de bug real (fonte de ícone não carregava) |
| 2026-09-21 | Head | **Home e hub "Mensageiros" mesclados em uma única página.** A pedido do Head, o passo intermediário (Home só com Prática da Semana + botão "MENSAGEIROS" → tela separada com os 4 botões de categoria) foi eliminado: agora a Home mostra a Prática da Semana e, logo abaixo, os botões de Orações/Músicas/Textos/Livros diretamente, sem toque extra. `MensageirosScreen.tsx` foi removido, a rota `Mensageiros` saiu de `RootStackParamList`/linking, e o ícone de Configurações (antes no cabeçalho de "Mensageiros") migrou para o cabeçalho da Home. O emblema "Chama Tripla" foi mantido como estava. | Um nível a menos de navegação hierárquica — trocado por acesso mais direto às categorias, a pedido explícito do Head |
| 2026-09-21 | Head | **Botões do hub Mensageiros: largura fixa (não esticam mais 100% da tela), formato pílula, com ícone minimalista por categoria e estilo aproximado de referência visual enviada (pílula escura, ícone + texto, borda sutil).** Ícones via `@expo/vector-icons` (MaterialCommunityIcons, já incluso como dependência transitiva do Expo, promovido a dependência direta): `hands-pray` (Orações), `music-note-outline` (Músicas), `text-box-outline` (Textos), `book-open-page-variant-outline` (Livros). Botões limitados a `maxWidth: 280`, centralizados na tela. | Nenhuma perda funcional — ~2.5MB extras no bundle web pelas fontes de ícone completas do `@expo/vector-icons` (inclui todos os sets, não só os 4 glyphs usados); otimizável depois com subsetting se o tamanho do bundle virar problema real |
| 2026-09-21 | Head | **Paleta trocada de "Mystery and transcendence" (fundo roxo) para "Ceremony and legacy in education".** O fundo violeta pálido `#D2D8FF` da paleta anterior foi reprovado pelo Head ("o fundo roxo não ficou bom"). Nova paleta: fundo Pale apricot `#FFEDDF` (creme claro, não-branco), texto Deep mint `#00170C`, primária Mulberry `#803C7A` (tom de violeta mantido nos acentos/botões, não no fundo), secundária Magenta `#AF65A8`, destaque Sage `#3BCFA9`. Tipografia trocada de Space Grotesk+Redaction para Bodoni Moda (títulos, serifado dramático) + Lexend (corpo, sans geométrica) — as três fontes da nova paleta (Bodoni Moda, Lexend, DM Mono) são Google Fonts, mais simples de carregar que a anterior (Redaction/League Mono exigiam Fontsource). | Nenhum — troca direta de paleta a pedido do Head, mesma mecânica de carregamento web (`src/theme/loadWebFonts.ts`) |
| 2026-09-21 | Head | **Cabeçalho fixo "Mensageiros da Paz" em todas as páginas.** Substitui os títulos por tela (nome da subpágina ou do item aberto) por um único título estático no topo, em toda a navegação (`RootNavigator.tsx`), incluindo a Home (antes sem cabeçalho nativo). | Orientação contextual do título por tela (saber em qual subpágina/item está só pelo cabeçalho) — trocada por identidade de marca mais forte e consistente no topo |
| 2026-09-21 | Head | **Nova paleta e tipografia — esquema Goethe, referência "Mystery and transcendence in education".** Substitui a paleta original (Areia Suave/Azul Horizonte/Terracota Suave, Fraunces+Inter) por: fundo Pale violet `#D2D8FF`, texto Deep terracotta `#270000`, primária Sprout `#4B4C00`, secundária Mustard `#838338`, destaque Sealing wax `#FC9997`; tipografia Space Grotesk (títulos) + Redaction (corpo), carregadas via Google Fonts/Fontsource só na web (`src/theme/loadWebFonts.ts`) — nativo ainda cai no fallback de sistema, sem arquivo de fonte bundled. O emblema "Chama Tripla" (ícone/favicon/Home) foi mantido como está, sem recolorir. | Consistência com a identidade visual documentada em `docs/CREATIVE-DIRECTION.md` (agora desatualizada quanto a cor/tipografia) e suporte nativo completo de fonte customizada (Redaction/League Mono não têm pacote `@expo-google-fonts`, só funcionam via CSS na web) — trocados a pedido explícito do Head por uma paleta/tipografia de referência externa |
| 2026-09-21 | Head | **Auditoria de código pré-lançamento (`audit-code`) — 4 achados bloqueantes corrigidos.** (1) Categoria "Livros" não tinha navegação nenhuma no app (18 PDFs cadastráveis pelo admin, inacessíveis a qualquer usuário) — adicionado botão "LIVROS" no hub Mensageiros. (2) `EMPTY_MESSAGE` em `ContentListScreen.tsx` usava `Record<string,string>` solto, sem cobrir `"livros"` — corrigido para `Record<ContentCategory,string>`, agora o compilador pega esquecimentos futuros. (3) Build do Cloudflare Pages não tinha roteamento SPA (`_redirects`) — acessar `/admin` direto (recarregar ou compartilhar link) retornava 404 antes do JS carregar; adicionado `app/public/_redirects` (`/* /index.html 200`), copiado automaticamente pelo `expo export`. (4) Build não copiava `app/content-src/` (áudio próprio) para `dist/` — único item de música com upload próprio do catálogo real ficava 404 em produção; criado script `npm run build:web` (`expo export -p web && cp -r content-src dist/content`), documentado como o comando de build correto do Cloudflare Pages em `docs/BACKEND-ARCHITECTURE.md`. Também corrigida a documentação do índice composto do Firestore (`category`+`published`+`order`, não só `category`+`order` como estava escrito). | Nenhum — eram bugs reais que impediriam funcionalidade prometida (Livros, música própria, link direto de admin) assim que o conteúdo fosse cadastrado; ESLint quebrado (sem `eslint.config.js`) fica registrado como pendência não bloqueante, não corrigido nesta rodada |
| 2026-09-19 | Head | **Emblema "Chama Tripla" definido como marca real** — substitui os ícones placeholder gerados anteriormente (círculo azul sólido). Aplicado como ícone do app (`icon.png`, `adaptive-icon.png`), favicon do site (`favicon.png`) e emblema visível na Home (`app/assets/branding/chama-tripla.png`), acima do texto "Prática da semana". Fundo original branco removido (transparência real, não só recorte), imagem cortada nas bordas do conteúdo. | Nenhum — troca direta de placeholder por asset de marca real, sem custo nem complexidade adicional |
| 2026-09-19 | Head | **Arquivo aceita qualquer provedor de cloud; streaming generalizado além do Spotify.** Dois ajustes ao modelo de conteúdo: (1) o campo de arquivo (`source: "upload"`) já aceitava qualquer link direto — deixado explícito na UI do admin e na documentação que Dropbox, OneDrive, etc. funcionam, não só Google Drive/Cloudflare; (2) `source: "spotify"` generalizado para `source: "streaming"` com campo `streaming_provider` (`spotify`/`youtube`/`soundcloud`/`apple_music`/`other`) — só Spotify tem player embutido (iframe/WebView), as demais abrem link externo com botão "Abrir no {plataforma}". | Simplicidade de um único tipo de streaming suportado — trocada por não travar o cadastro de música a uma única plataforma, já que o Head pode ter conteúdo espalhado entre vários serviços |
| 2026-09-19 | Head | **Painel de admin reintroduzido, dentro do próprio app (`/admin`).** Reverte a decisão de 2026-09-18 ("sem painel web dedicado, usar Firebase Console direto") — na prática o Console se mostrou pouco prático pro Head. Nova tela `AdminScreen` no mesmo código-base (React Native/Expo), acessível em `https://mensageiros-da-paz.pages.dev/admin` na web (React Navigation linking) e por um link discreto em Configurações no app nativo. Login por e-mail/senha (conta única, criada manualmente pelo Head no Firebase Authentication — sem self-signup). Permite editar a Prática da Semana e fazer CRUD de itens (Orações/Músicas/Textos/Livros) — mas sem upload de arquivo de verdade (ainda sem Storage): o campo de arquivo é um link colado (Google Drive ou Cloudflare Pages), não um upload arrastar-e-soltar. Security Rules do Firestore precisam exigir especificamente `sign_in_provider == 'password'` (não só `request.auth != null`), porque o app já usa autenticação anônima para device tokens de push — sem essa distinção, qualquer visitante do app teria permissão de escrita. | Simplicidade "zero manutenção" da abordagem anterior (só o Console, nenhum código extra pra manter) e upload de arquivo com clique-arrastar — trocados por uma experiência de edição muito mais rápida para o único operador (o Head), aceitando colar links em vez de upload direto |
| 2026-09-19 | Head | **Sem Firebase Storage — PDFs no Google Drive, áudio no Cloudflare Pages.** Desde fev/2026 o Firebase Storage exige o plano Blaze (cartão cadastrado), mesmo dentro da faixa gratuita — o Head recusou cadastrar cartão. Firestore continua 100% grátis no Spark (confirmado), então só o armazenamento de arquivo mudou de lugar: os 18 PDFs de "Livros" ficam hospedados no Google Drive do Head (links "Qualquer pessoa com o link"), e arquivos de áudio próprios (ex. "Guerreiro do Bem") ficam versionados no repositório e servidos estaticamente pelo próprio Cloudflare Pages. `file_url` em `items` aponta para essas fontes em vez de um bucket do Firebase Storage — o app não muda de código (já lê `file_url` como string genérica). | Confiabilidade de streaming de áudio inline (Drive não garante headers CORS/Content-Type consistentes para tocar áudio embutido — por isso áudio próprio vai para o Cloudflare, não Drive) e a conveniência de um único provedor de storage — trocados por continuar 100% sem custo e sem cartão cadastrado |
| 2026-09-19 | Head | **Nova categoria "Livros" dentro de Leituras, com 18 PDFs reais.** O botão "Textos" foi renomeado para "Leituras" e passa a ser um hub com duas seções: Textos e Livros. Livros lista 18 PDFs reais fornecidos pelo Head (tradição esotérica/espiritualista diversa), cada um como card com ícone, título e descrição de duas linhas, abrindo para leitura no próprio dispositivo. Modelo de dados estendido com `category: "livros"` em `docs/BACKEND-ARCHITECTURE.md`. Ícones são ilustração vetorial genérica (sem capa real de cada livro — não há fonte confiável de capa para todos os títulos). Leitura de PDF dentro do app (não só link externo) depende de `react-native-pdf` rodando em build nativo EAS — ainda não implementado no código, só simulado no protótipo visual. | Capas de livro reais e leitor de PDF nativo funcionando hoje — trocados por um ícone consistente da marca e um protótipo visual fiel, com a implementação real do leitor ficando como próximo passo de engenharia |
| 2026-09-19 | Head | **Player do Spotify embutido (iframe/WebView), não link externo.** Retificação da entrada anterior de mesma data: o Head pediu explicitamente o player embutido do Spotify, não apenas um link que abre o app externo. Implementado via `react-native-webview` no app nativo e `<iframe>` na versão web (`app/src/components/SpotifyEmbed.tsx`/`.web.tsx`). **Efeito colateral sobre a decisão de custo zero de 2026-09-18:** `react-native-webview` é um módulo nativo — o player embutido só funciona em build nativo via EAS (dev client), não no Expo Go puro. Na prática, isso significa: no Android via APK direto (build EAS) funciona normalmente; no Expo Go (usado hoje para iOS sem custo) o player embutido não vai carregar — precisa ser testado e, se necessário, cair num fallback de link externo só nesse caso específico. | Simplicidade de manter tudo rodando em Expo Go puro sem nenhum build nativo — trocada por ter o player de música de verdade embutido no app, como pedido |
| 2026-09-19 | Head | **Músicas: conteúdo misto — upload próprio + Spotify, por enquanto.** A categoria "Músicas" aceita tanto arquivo de áudio próprio (upload no Firebase Storage, tocado pelo player nativo) quanto link de faixa do Spotify (abre no app/site do Spotify, sem player embutido nem integração OAuth). Modelo de dados estendido com o campo `source: "upload" \| "spotify"` em `docs/BACKEND-ARCHITECTURE.md` seção 2.2. | Consistência de experiência (algumas faixas tocam dentro do app, outras saem para o Spotify) e simplicidade de um único tipo de conteúdo — trocados por não depender de licenciar/hospedar toda música e poder linkar conteúdo já disponível no Spotify |
| 2026-09-18 | Head | **Push notifications: só Android por enquanto, custo total US$0.00.** Conforme `docs/DISTRIBUTION-PLAN.md`, o Head optou por não pagar a conta Apple Developer Program (~US$99/ano) agora. iOS roda via Expo Go (sem push nativo funcional); Android roda via APK direto com push funcionando. Isso implica: (1) `mobile-crossplatform` deve simplificar o registro de push para usar o Expo Push Token (via `expo-notifications`) em vez de exigir `@react-native-firebase/messaging`/build nativo EAS, já que não há TestFlight nem dev client nativo neste momento; (2) a Cloud Function `sendPush` (`docs/BACKEND-ARCHITECTURE.md`) deve enviar via Expo Push API (que já roteia para FCM no Android) em vez de Firebase Admin Messaging direto; (3) a tela de configurações com o toggle de notificações deve deixar claro, quando rodando em iOS/Expo Go, que push não está disponível nessa plataforma agora. Reavaliar se o grupo crescer ou se o Head decidir pagar a conta Apple no futuro (ver `docs/DISTRIBUTION-PLAN.md`). | Notificações no iPhone (adiadas) e o caminho "mais robusto" de token FCM nativo — trocados por custo zero e menor complexidade de build, adequado ao uso fechado/pessoal já decidido |
| 2026-09-18 | Head | **Restrição de escopo: uso pessoal/grupo fechado, sem custo de servidor, publicação ou manutenção.** O app é para o Head e pessoas cadastradas manualmente por ele — não é um produto público a ser publicado nas lojas para o público geral. Isso rebaixa/elimina bloqueadores da auditoria de deploy (`docs/DEPLOY-READINESS-AUDIT.md`) que assumiam produto público: painel de admin web dedicado (pode virar edição direta no console do Firebase), Cloud Functions com rate limiting sofisticado (lista fechada de usuários reduz o risco de abuso), e submissão pública nas lojas (distribuição pode ser fechada: TestFlight interno / Google Play testes internos / instalação direta do APK, evitando ou adiando taxas de loja). Mantém-se, porém, como custo inevitável caso se quera instalar em iPhone físico fora do simulador: a conta Apple Developer Program (~US$99/ano) — não há alternativa gratuita da Apple para isso. `backend-integration` e `devops-deploy` devem revisar `docs/BACKEND-ARCHITECTURE.md` e o plano de distribuição à luz desta restrição. | Escalabilidade, robustez contra abuso em escala e onboarding self-service de novos usuários — trocados por custo zero/mínimo e complexidade reduzida, adequado ao uso fechado declarado |
| 2026-09-18 | Head | **Uso de dado pessoal — device token FCM para push aprovado com ressalvas**, conforme parecer de `security-privacy` em `docs/PRIVACY-REVIEW.md`. Base legal: consentimento explícito (opt-in, desativado por padrão). Ajustes obrigatórios antes do gate de produção: rota de exclusão explícita de token ao desativar o toggle, job periódico de limpeza de tokens obsoletos, validação server-side estrita do payload de registro de device, Política de Privacidade do app, e declarações de privacidade nas lojas (Apple/Google). Validação jurídica formal da conformidade LGPD permanece de responsabilidade do Head, fora do escopo técnico do time. | Lançamento imediato sem os ajustes acima — trocado por conformidade mínima defensável e respeito à minimização de dados (só token + plataforma, nada de PII) |
| 2026-09-18 | Head | **Stack mobile: React Native + Expo** (managed workflow com EAS Build para incluir módulos nativos de push), recomendada por `mobile-crossplatform`, para um único código-base iOS+Android consumindo Firebase (Auth/Firestore/Storage/FCM) já decidido para o backend. Critérios: (1) time e ferramentas do produto já são JS/TS-first (painel admin web também é React, ver `docs/BACKEND-ARCHITECTURE.md`), reduzindo troca de contexto; (2) Firebase JS SDK (`firebase/app`, `firebase/firestore`) funciona out-of-the-box no Expo managed workflow para Firestore/Storage, sem precisar de config nativa complexa; (3) push via FCM exige `expo-notifications` + `@react-native-firebase/messaging` com **EAS Build** (dev client customizado, não Expo Go puro) — aceito porque ainda é um único código-base e o custo de setup é pequeno frente a nativo puro; (4) Flutter foi descartado por exigir Dart (fora do stack do time) sem ganho relevante de performance para 5 telas simples de leitura/lista; nativo (Swift+Kotlin) foi descartado por dobrar o esforço de engenharia para um MVP de baixa complexidade de UI (sem animação 3D, sem processamento pesado). Ver `app/README.md`. | Performance/controle nativo total (perdidos frente a Swift/Kotlin) e algum atrito inicial de configuração de push (EAS Build em vez de Expo Go simples), em troca de um único código-base, menor custo de manutenção e reaproveitamento do skill JS/TS do time para o MVP |
| 2026-09-18 | Head | **Stack de backend/hosting: Firebase** (Auth, Firestore, Storage, Cloud Functions, Firebase Cloud Messaging), recomendada por `backend-integration` para viabilizar admin de conteúdo (Prática da Semana + upload PDF/JPG/PNG/áudio em Orações/Músicas/Textos) e push nativo iOS/Android com o menor esforço de infraestrutura própria. Ver `docs/BACKEND-ARCHITECTURE.md`. | Portabilidade/lock-in no ecossistema Google e modelagem relacional verdadeira (perdidas frente a Supabase/backend custom), em troca de velocidade de entrega do MVP e suporte nativo unificado a push (FCM cobre APNs e Android) |
| 2026-09-16 | Head | **Ativar estrutura completa (18 papéis)** a pedido explícito do usuário, com perfis de senioridade verbatim; apagados os 6 subagentes fundidos do Estágio Enxuto e refeitos como especialistas individuais. Head permanece como sessão principal (não subagente). | Menor custo de coordenação do Estágio Enxuto — trocado por granularidade 1-para-1 com o organograma |
| _(anterior)_ 2026-09-16 | Head | Estrutura em Estágio Enxuto (6 papéis fundidos) — **substituída** | Granularidade de 18 papéis, em troca de menor custo de coordenação |

| 2026-09-21 | Head | **Fonte de título trocada de Bodoni Moda para Lora — legibilidade no mobile.** Bodoni Moda (contraste alto, x-height baixo, estilo "moda/editorial") ficava difícil de ler nos títulos das telas em tamanho pequeno de celular (relatado pelo Head com print da lista de Orações). Trocada por Lora — serifada "de livro" desenhada pra tela, com x-height maior e contraste mais suave, mantendo elegância e combinando com o corpo em Lexend. Aplicado em `fonts.display`/`fonts.displayFallback` (`app/src/theme/tokens.ts`) e no `<link>` de Google Fonts (`app/src/theme/loadWebFonts.ts`) — nenhum outro arquivo referenciava a fonte diretamente. | Estética "moda/editorial" de Bodoni Moda — trocada por legibilidade real em tela pequena, sem perder o caráter de serifada elegante nos títulos |

| 2026-09-21 | Head | **Guia do Admin embutido: link do artifact claude.ai trocado por cópia estática no próprio site.** A 1ª tentativa embutiu a página via `<iframe>` apontando pro link do artifact (`claude.ai/artifact/...`) — confirmado em produção que o claude.ai recusa ser embutido em iframe de outro domínio ("claude.ai refused to connect", X-Frame-Options/CSP). Corrigido copiando o HTML do guia pra `app/public/guia-admin.html`, servido como arquivo estático do próprio Cloudflare Pages (mesmo domínio do app — sem restrição de iframe) em `mensageiros-da-paz.pages.dev/guia-admin.html`. Os botões de exportação `.md` ganharam fallback de download comum (link `blob:`) pra essa cópia, já que a capability `downloads` do claude.ai (usada na versão artifact) só existe dentro do viewer do claude.ai. **Decisão do Head no mesmo dia:** manter só uma cópia — `app/public/guia-admin.html` é a fonte única do Guia do Admin daqui pra frente; o link do artifact claude.ai foi descontinuado como referência (deixou de ser atualizado), evitando o trabalho manual de sincronizar duas cópias a cada mudança de conteúdo. | Link avulso do artifact pra compartilhar o guia fora do app — trocado por uma única fonte de verdade, mais simples de manter |

| 2026-09-21 | Head | **Itens criados via "Publicação manual" passam a sincronizar com a planilha da categoria.** A pedido do Head: item importado de planilha já fica lá, mas item cadastrado direto no painel (sem passar pela planilha) ficava só no Firestore, sem registro na planilha correspondente. Implementado `app/src/integrations/contentSheetSync.ts`, mesmo mecanismo já usado pra Prática da Semana (Google Apps Script Web App por planilha, gratuito) — 4 variáveis novas (`EXPO_PUBLIC_SHEET_WEBHOOK_ORACOES/MUSICAS/TEXTOS/LIVROS`), passo a passo completo em `docs/CONTENT-SHEET-SETUP.md`. **Limitação técnica registrada:** não existe ferramenta de escrita direta em planilha do Google Sheets disponível nesta sessão (só leitura/criação de arquivo via Google Drive) — não foi possível adicionar linhas retroativas nas planilhas reais nem confirmar o nome exato das colunas de Orações/Músicas; o script usa a convenção já documentada no Guia do Admin, ajustável pelo Head se divergir da planilha real. | Sincronização automática/retroativa via API — trocada por depender de um deploy manual do Apps Script (mesmo padrão já em uso, sem custo, sem credencial nova) |

| 2026-09-21 | Head | **`_redirects` corrigido — sintaxe `200!` (forçar) é do Netlify, não do Cloudflare Pages.** Achado no log de um deploy comum ("Found invalid redirect lines... Parsed 0 valid redirect rules"): a regra de fallback SPA (`/* /index.html 200!`) nunca foi válida no Cloudflare — o `!` não é reconhecido e a regra inteira era descartada. Corrigido pra `/* /index.html 200` (sem "!"), sintaxe válida no Cloudflare Pages. Acesso direto a rotas como `/admin` provavelmente já funcionava por comportamento padrão do próprio Cloudflare Pages pra 404, não pela regra escrita — agora a regra existe de fato. | Nenhuma — corrige uma regra que nunca funcionou, sem mudar comportamento esperado |

| 2026-09-21 | Head | **Sync com a planilha estendido: agora reflete editar e apagar, com colunas Formato e Status.** Antes só a criação via "Publicação manual" sincronizava (sempre `appendRow`). A pedido do Head: editar (de qualquer item, inclusive importado) e apagar um item no painel também tentam refletir na planilha — o Apps Script de cada categoria agora procura a linha pelo título antes de agir (`action: "create"/"update"/"delete"`), atualiza os valores numa edição, e num "apagar" só marca a coluna **Status** como "Deletado" (nunca remove a linha, mantém histórico). Duas colunas novas em cada planilha (Formato, Status) — precisam ser adicionadas manualmente pelo Head antes de reimplantar os 4 scripts, documentado em `docs/CONTENT-SHEET-SETUP.md`. | Simplicidade do só-adicionar-linha anterior — trocada por refletir o estado real do item (editado/apagado), a pedido explícito do Head |
| 2026-09-21 | Head | **Causa raiz do sync "não funcionar" era o deploy, não o código.** Depuração ao vivo (rodada de testes com DevTools) mostrou 0 chamadas de rede pro Apps Script mesmo após configurar a variável de ambiente certa — rastreado até "Retry deployment" no Cloudflare Pages reexecutar o COMMIT CONGELADO daquela linha específica da lista de Deployments, não o HEAD atual do branch; a Production ficou presa num commit anterior à correção mesmo com deploys novos (e corretos) tendo passado no meio do caminho. Resolvido com um commit vazio forçando deploy novo do HEAD real. Lição registrada em `docs/CONTENT-SHEET-SETUP.md`: nunca usar "Retry" numa linha antiga pra forçar atualização de variável de ambiente — sempre um push novo. | Nenhuma — achado operacional, não uma escolha de trade-off |

<!-- Adicione novas decisões acima desta linha, a mais recente no topo. -->
